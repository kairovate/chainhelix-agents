{
  "name": "agent-marketplace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Agent marketplace data core: one source of truth serving the JSON API (this service), the web pages and the MCP surface.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "fastify": "^5.12.3"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
